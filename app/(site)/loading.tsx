"use client";

import Box from "@/components/Box";

const Loading = () => {
  return (
    <Box className=" text-gray-900 flex justify-center items-center h-full ">
      <div>loading....</div>
    </Box>
  );
};

export default Loading;
